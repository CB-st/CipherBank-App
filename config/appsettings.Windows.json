{
  "_comment": "Windows overlay. Merged after the environment overlay when Build(windowsOverlay: true). Persistence.DatabaseName is the documented, testable override.",
  "Persistence": {
    "DatabaseName": "cipherbank.win.db"
  }
}
