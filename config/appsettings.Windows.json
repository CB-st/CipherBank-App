// Windows overlay. Merged after the environment overlay when Build(windowsOverlay: true).
// Persistence.DatabaseName is the documented, testable override.
// Add section overrides here instead of forked JSON files.
{
  "Persistence": {
    "DatabaseName": "cipherbank.win.db"
  }
}
