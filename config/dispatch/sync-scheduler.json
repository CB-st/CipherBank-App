{
  "_comment": "Controls TaskScheduler-backed sync dispatch. Keep concurrency bounded on mobile devices.",
  "SyncScheduler": {
    "_comment": "Omit MaxConcurrency to keep MinConcurrency (1). Set MaxConcurrency here only to override (max 8)."
  }
}
