{
  "_comment": "Development overlay. Merged after appsettings.json when Build(environment: Development) is requested. Persistence.DatabaseName is the documented, testable override.",
  "Persistence": {
    "DatabaseName": "cipherbank.dev.db"
  }
}
