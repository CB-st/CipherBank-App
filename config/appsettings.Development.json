{
  "_comment": "Development overlay. Merged after appsettings.json when the host environment is Development. Add section overrides here (e.g. Persistence.DatabaseName) instead of forked JSON files."
}
