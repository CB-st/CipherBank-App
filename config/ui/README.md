# UI Configuration

`cora-lines.json` contains localizable UI copy keyed by screen. `carousel.json`
contains device-independent layout and motion defaults; values are inputs to the
pure Core math and can be overridden without changing that math.
