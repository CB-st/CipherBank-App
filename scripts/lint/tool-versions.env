# Pinned versions for scripts/lint/install-tools.sh
# Bump deliberately; keep comments when changing.

SHELLCHECK_VERSION=0.10.0
RUFF_VERSION=0.12.4
CHECKMAKE_VERSION=0.2.2

# clang-tidy / clang-format: prefer distro or LLVM apt packages.
# No single portable version pin — install-tools verifies presence only.
CLANG_TIDY_MIN_HINT="14"
