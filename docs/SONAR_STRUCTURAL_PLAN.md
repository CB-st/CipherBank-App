# Sonar Stage 2 — Structural Plan (SA1402 / SA1649)

**Status: STAGE 2 SPLITS COMPLETE on `prototype/maui-m4` (2026-07-26).** All planned SA1402/SA1649 rows executed; callers remain documented for merge-up to m1–m3. This document
inventories every `SA1402` ("file may only contain a single type") and
`SA1649` ("file name should match first type name") issue reported against
`prototype/maui-m1` (PR #20) and `prototype/maui-m2` (PR #21) in
`/tmp/sonar-stack/{m1,m2}/issues.json`, and proposes a split target for each.
Per the Stage 1 spec, **Stage 3 (medium/minor/info) does not start until this
plan is reviewed**, and no code movement should land until each row's
"Proposed path" is turned into a real task with tests.

Source data: `external_roslyn:SA1402` + `external_roslyn:SA1649` entries only.
Type lists are best-effort from the tree; `*` marks the type Sonar/StyleCop treats as "first"
(the one the filename should match). **Callers** were filled 2026-07-26 via `\bTypeName\b`
grep across Core, ChallengePass, Shell, Tests, and E2E.

## Legend

- **Breaks** — expected blast radius of the proposed split, not yet verified by build/test.
  - `rename only` — file renamed to match its sole/primary type; no type or namespace change; zero API impact.
  - `split only` — extra types extracted to sibling files in the same namespace; no rename, no API impact.
  - `rename + split` — both of the above.
  - `high churn` — module used widely across `ChallengePass`; coordinate before executing.
- **Annotation needed** — `Yes`: every new/renamed file must get the AGENTS.md copyright header (updated `file="..."` to match new name) and, for any touched function, the mandatory purpose/call-frequency/scope doc comment. This column is about the **cross-PR duplication comments** reviewers will see: it flags that reviewing the header/doc-comment diff once, on the owning layer where the split first lands, is sufficient — the identical diff reappearing on each downstream PR (`m1` → `m2` → `m3` → `m4`) as the split merges up is expected merge noise, not a fresh review item, and should not be re-litigated on every branch.

## Open before Stage 2 approval

- **Callers filled (2026-07-26)** via `\\bTypeName\\b` grep across Core, ChallengePass, Shell, Tests, and E2E on `prototype/maui-m4`. `Breaks` remains an estimate until each split is build-verified. Re-check Shell compile gate after every batch.
- **Shell compile gate.** The caller search above must include the MAUI Shell (`CipherBank-app/` — view models, platform services, and DI registration), not just `Core` and `ChallengePass`. Stage 1 already produced one Critical finding of exactly this shape: Core API signatures changed without mapping Shell call sites, and the Shell stopped compiling even though `Core` and the 267-test suite were green. The fix landed as **zero-token default interface methods (DIMs)** on the affected `Core` interfaces (`IProductClient`, `IStepUpAuth`, `IMnemonicBackupService`, `IWalletService`, etc.), forwarding to the `CancellationToken`-required member — that is the **Stage 1 compatibility strategy** for keeping interface-typed call sites source-compatible without reintroducing optional parameters on abstract interface slots. Renames and type moves in Stage 2 have the same blast radius, so **every Stage 2 PR must build `CipherBank-app/CipherBank-app.csproj`, not only run `dotnet test`**, and the caller mapping in step 2 must explicitly cover Shell ViewModels (the layer where the Stage 1 break actually surfaced). Note the Shell only exists in full from `prototype/maui-m3` upward, so the compile gate runs there even when the split itself lands on `m1`.

## CipherBank-app.Core

| Layer | File | Types (best-effort, `*`=primary) | Callers | Proposed path(s) | Breaks | Annotation needed |
|---|---|---|---|---|---|---|
| Core/Charts | `Charts/ChartMath.cs` **DONE extract ChartPoint, ChartPathResult** | ChartPoint(record), ChartMath(class)*, ChartPathResult(class) | Shell: CompareChartDrawable, HomeViewModel, SparklineDrawable; Tests: ChartMathTests; E2E: none | `Charts/ChartPoint.cs`, `Charts/ChartPathResult.cs` | split only | Yes |
| Core/Custody | `Custody/CustodyService.cs` **DONE extract CustodyPinChangeResult, ICustodyService** | CustodyPinChangeResult(enum), ICustodyService(interface), CustodyService(class)* | Core: AppSession, PinChange; CP: CustodyAccountKeySource; Shell: AddWalletViewModel, MauiProgram, ProfileViewModel, ReceiveViewModel, RestoreBackupViewModel, WelcomeViewModel; Tests: AppSessionTests, BiometricUnlockContractTests, CustodyAccountKeySourceTests, PinAndCustodyTests, PinChangeTests; E2E: none | `Custody/CustodyPinChangeResult.cs`, `Custody/ICustodyService.cs` | split only | Yes |
| Core/Custody | `Custody/MnemonicHelper.cs` (was Mnemonic.cs) **DONE** | MnemonicHelper(class)* | Core: AddressDerive, CustodyService, MnemonicBackupService; CP: CustodyAccountKeySource; Shell: BackupQuizViewModel, KeysViewModel, SetPinViewModel; Tests: AppSessionTests, BiometricUnlockContractTests, CustodyAccountKeySourceTests, MnemonicBackupServiceTests, MnemonicTests, PinAndCustodyTests, PinChangeTests, WalletRegistryTests; E2E: AccountStories | rename file → `Custody/MnemonicHelper.cs` | rename only | Yes |
| Core/Custody | `Custody/PinChangeStatus.cs` (was PinChange.cs) **DONE** | PinChangeStatus(enum)*, PinChangeOutcome(record), PinChangeCoordinator(class) | Shell: ChangePinViewModel, MauiProgram; Tests: PinChangeTests; E2E: StoryCatalog (doc ref only) | rename file → `Custody/PinChangeStatus.cs`; extract `Custody/PinChangeOutcome.cs`, `Custody/PinChangeCoordinator.cs` | rename + split | Yes |
| Core/Custody | `Custody/PinService.cs` **DONE extract IPinService** | IPinService(interface), PinService(class)* | Core: CustodyService, PinChange, StepUpAuthService; Shell: MauiProgram, ProfileViewModel, UnlockViewModel; Tests: AppSessionTests, BiometricUnlockContractTests, CustodyAccountKeySourceTests, PinAndCustodyTests, PinChangeTests, StepUpAuthTests; E2E: none | `Custody/IPinService.cs` | split only | Yes |
| Core/Persist | `Persist/HoldingVisibility.cs` **DONE extract HoldingVisibilityResult** | HoldingVisibility(class)*, HoldingVisibilityResult(record) | Shell: HomeViewModel; Tests: HoldingVisibilityTests; E2E: none | `Persist/HoldingVisibilityResult.cs` | split only | Yes |
| Core/Persist | `Persist/IRatesCache.cs` **DONE extract RateRow** | IRatesCache(interface)*, RateRow(record) | Core: MarketBootstrap, RatesCache; Shell: HomeViewModel, MauiProgram; Tests: MarketBootstrapTests, RatesCacheTests; E2E: none | `Persist/RateRow.cs` | split only | Yes |
| Core/Persist | `Persist/LocalDb.cs` **DONE extract ILocalDb** | ILocalDb(interface), LocalDb(class)* | Core: MarketRepository, PrefsStore, RatesCache, RecipientRepository, WalletRepository; Shell: App.xaml, AppShell.xaml, MauiProgram; Tests: LocalDbMigrationTests, MarketRepositoryTests, PrefsStoreTests, RatesCacheTests, RecipientRepositoryTests; E2E: none | `Persist/ILocalDb.cs` | split only | Yes |
| Core/Persist | `Persist/PrefsStore.cs` **DONE extract IPrefsStore, UserPrefs** | IPrefsStore(interface), UserPrefs(class), PrefsStore(class)* | Core: AccountBootstrapService, AppSession, HoldingVisibility, PrefsSyncService, PrefsWire; Shell: CoraBar, CoraFab, HomeViewModel, MauiProgram, ProfileViewModel, SendViewModel; Tests: AppSessionTests, CoreZeroTokenOverloadTests, PrefsNormalizeTests, PrefsStoreTests, PrefsSyncAndStreamTests; E2E: none | `Persist/IPrefsStore.cs`, `Persist/UserPrefs.cs` | split only | Yes |
| Core/Persist | `Persist/RecipientRepository.cs` **DONE extract IRecipientRepository, AchRecipientRow** | IRecipientRepository(interface), AchRecipientRow(record), RecipientRepository(class)* | Core: AccountBootstrapService; Shell: MauiProgram, SendViewModel; Tests: AppSessionTests, RecipientRepositoryTests; E2E: none | `Persist/IRecipientRepository.cs`, `Persist/AchRecipientRow.cs` | split only | Yes |
| Core/Persist | `Persist/WalletRepository.cs` **DONE extract IWalletRepository, LocalWalletRow** | IWalletRepository(interface), LocalWalletRow(record), WalletRepository(class)* | Core: LocalWalletSeeder; Shell: AddWalletViewModel, HomeViewModel, MauiProgram, ReceiveViewModel; Tests: AppSessionTests; E2E: none | `Persist/IWalletRepository.cs`, `Persist/LocalWalletRow.cs` | split only | Yes |
| Core/Pos | `Pos/INfcPresentmentService.cs` (was NfcPresentment.cs) **DONE** | INfcPresentmentService(interface)*, EmvExchangeSimulator(class), NfcPresentmentPayload(class), NullNfcPresentmentService(class) | Shell: AndroidNdefPresentmentService, MauiProgram, PosLabViewModel; Tests: CoreZeroTokenOverloadTests, NfcPresentmentPayloadTests; E2E: none | rename file → `Pos/INfcPresentmentService.cs`; extract `Pos/EmvExchangeSimulator.cs`, `Pos/NfcPresentmentPayload.cs`, `Pos/NullNfcPresentmentService.cs` | rename + split | Yes |
| Core/Session | `Session/AppSession.cs` **DONE extract IAppSession** | IAppSession(interface), AppSession(class)* | Shell: App.xaml, AppIdleLockService, AppShell.xaml, ChangePinViewModel, ConvertViewModel, HomeViewModel, MauiProgram, PayViewModel, PosLabViewModel, ProfileViewModel, ReceiveViewModel, SendViewModel, SetPinViewModel, UnlockViewModel; Tests: AppSessionTests; E2E: none | `Session/IAppSession.cs` | split only | Yes |
| Core/V1 | `V1/AccountBootstrap.cs` **DONE extract BootstrapRecipientDto** | AccountBootstrapDto(class)*, BootstrapRecipientDto(class) | Core: AccountBootstrapService, IProductClient, InMemoryProductClient; Shell: HttpProductClient; Tests: AppSessionTests, CoreZeroTokenOverloadTests, PrefsSyncAndStreamTests; E2E: none | `V1/BootstrapRecipientDto.cs` | split only | Yes |
| Core/V1 | `V1/AccountBootstrapService.cs` **DONE extract IAccountBootstrapService** | IAccountBootstrapService(interface), AccountBootstrapService(class)* | Core: AppSession; Shell: MauiProgram; Tests: AppSessionTests; E2E: none | `V1/IAccountBootstrapService.cs` | split only | Yes |
| Core/V1 | `V1/PrefsSyncService.cs` **DONE extract IPrefsSyncService** | IPrefsSyncService(interface), PrefsSyncService(class)* | Core: AppSession; Shell: MauiProgram, ProfileViewModel; Tests: AppSessionTests, CoreZeroTokenOverloadTests, PrefsSyncAndStreamTests; E2E: none | `V1/IPrefsSyncService.cs` | split only | Yes |
| Core/V1 | `V1/PrefsMerge.cs` (was PrefsWire.cs) **DONE** | PrefsMerge(class)*, PrefsWireDto(class) | Core: AccountBootstrap, AccountBootstrapService, IProductClient, InMemoryProductClient, PrefsSyncService; Shell: HttpProductClient; Tests: AppSessionTests, CoreZeroTokenOverloadTests, PrefsSyncAndStreamTests; E2E: none | rename file → `V1/PrefsMerge.cs`; extract `V1/PrefsWireDto.cs` | rename + split | Yes |
| Core/V1 | `V1/ISessionProofBuilder.cs` (was SessionChallenge.cs) **DONE** | ISessionProofBuilder(interface)*, SessionChallengeDto(class), SessionPassDto(class), LabSessionProofBuilder(class) | Core: IProductClient, InMemoryProductClient; CP: ChallengePassServiceCollectionExtensions, ChallengePassSessionProofBuilder, ISessionChallengeClient, InMemorySessionChallengeClient, PqChannelChallengePassStructure, TwoStepChallengePassStructure; Shell: HttpPqChannelChallengeSource, HttpPqKeyShareClient, HttpProductClient, HttpSessionChallengeClient, MauiProgram; Tests: AppSessionTests, ChallengePassModuleTests, ChallengePassZeroTokenOverloadTests, CoreZeroTokenOverloadTests, HybridPqChannelTests, SessionProofBuilderTests; E2E: none | rename file → `V1/ISessionProofBuilder.cs`; extract `V1/SessionChallengeDto.cs`, `V1/SessionPassDto.cs`, `V1/LabSessionProofBuilder.cs` | rename + split | Yes |
| Core/V1 | `V1/KeyShareRequestDto.cs` (was SessionKeyShareWire.cs) **DONE** | KeyShareRequestDto(class)*, KeyShareResponseDto(class), CreateWalletRequestDto(class), CreateWalletResultDto(class) | Core: IProductClient, InMemoryProductClient; Shell: AddWalletViewModel, HttpPqKeyShareClient, HttpProductClient; Tests: AppSessionTests, CoreZeroTokenOverloadTests, CreateWalletTests; E2E: none | rename file → `V1/KeyShareRequestDto.cs`; extract `V1/KeyShareResponseDto.cs`, `V1/CreateWalletRequestDto.cs`, `V1/CreateWalletResultDto.cs` | rename + split | Yes |
| Core/V1 | `V1/StreamHub.cs` **DONE extract IStreamHub** | IStreamHub(interface), StreamHub(class)* | Core: AppSession; Shell: ConvertViewModel, HomeViewModel, MauiProgram; Tests: AppSessionTests, PrefsSyncAndStreamTests; E2E: none | `V1/IStreamHub.cs` | split only | Yes |
| Core/V1 | `V1/IStreamService.cs` (was StreamService.cs) **DONE** | IStreamService(interface)*, StreamEvent(class), MockStreamService(class), ClientWebSocketStreamService(class) | Core: AppSession, StreamHub; Shell: ConvertViewModel, HomeViewModel, MauiProgram; Tests: AppSessionTests, PrefsSyncAndStreamTests; E2E: none | rename file → `V1/IStreamService.cs`; extract `V1/StreamEvent.cs`, `V1/MockStreamService.cs`, `V1/ClientWebSocketStreamService.cs` | rename + split | Yes |
| Core/V1 | `V1/PortfolioDto.cs` (was WireModels.cs) **DONE** | PortfolioDto(class)*, HoldingDto, HistoryPointDto, QuoteDto, SessionDto, ReceiveDto, PosSessionDto, VaultCardDto, MoneyMoveDto, VaultBinaryDto (all class) | Core: AppSession, HoldingVisibility, IProductClient, IProductSessionStore, InMemoryProductSessionStore, IndicativeQuoteMapper, InMemoryProductClient; Shell: ConvertViewModel, HomeViewModel, HttpProductClient, ProductSessionStorage, ProfileViewModel; Tests: AppSessionTests, CoreZeroTokenOverloadTests, HoldingVisibilityTests, InMemoryHistoryRangeTests, InMemoryVaultCardMutationTests; E2E: none | rename file → `V1/PortfolioDto.cs`; extract remaining 9 DTOs to their own `V1/<Name>.cs` | rename + split | Yes |
| Core/Wallets | `Wallets/LocalWalletSeeder.cs` **DONE extract ILocalWalletSeeder** | ILocalWalletSeeder(interface), LocalWalletSeeder(class)* | Core: AppSession; Shell: MauiProgram; Tests: AppSessionTests; E2E: none | `Wallets/ILocalWalletSeeder.cs` | split only | Yes |
| Core/Wallets | `Wallets/WalletRegistry.cs` **DONE extract WalletUiMode, WalletSource, WalletModule** | WalletUiMode(enum), WalletSource(enum), WalletRegistry(class)*, WalletModule(class) | Core: LocalWalletSeeder; Shell: AddWalletViewModel, ConvertViewModel, ReceiveViewModel; Tests: WalletRegistryTests; E2E: none | `Wallets/WalletUiMode.cs`, `Wallets/WalletSource.cs`, `Wallets/WalletModule.cs` | split only | Yes |

## CipherBank-app.ChallengePass

| Layer | File | Types (best-effort, `*`=primary) | Callers | Proposed path(s) | Breaks | Annotation needed |
|---|---|---|---|---|---|---|
| ChallengePass | `ISealAlgorithm.cs` (was ChallengePassSlots.cs) **DONE** | ISealAlgorithm(interface)*, IChallengeTemplate, IChallengePassStructure, IChallengePassCatalog (interfaces), AccountKeyPair(record), ChallengeBindContext, ParsedChallenge, ChallengePassSuite (class) | CP: 15 internal refs; Shell: MauiProgram; Tests: ChallengePassModuleTests, CustodyAccountKeySourceTests, HybridPqChannelTests; E2E: none | rename file → `ISealAlgorithm.cs`; extract the other 7 types to their own files | high churn | Yes |
| ChallengePass/Crypto | `Crypto/PortableX25519.cs` **DONE extract PortableChaCha20Poly1305** | PortableX25519(class)*, PortableChaCha20Poly1305(class) | CP: HybridMlKemX25519Agreement, PqSymmetricChannel, X25519ChaChaSealAlgorithm; Shell: none; Tests: none; E2E: none | `Crypto/PortableChaCha20Poly1305.cs` | split only | Yes |
| ChallengePass/Hybrid | `Hybrid/HybridKeyShareModels.cs` **DONE rename + split** | IPqKeyShareClient(interface)*, HybridPublicIdentity, HybridPrivateIdentity, PqKeyShareResponse (class) | CP: 9 refs; Shell: HttpPqKeyShareClient, MauiProgram; Tests: ChallengePassZeroTokenOverloadTests, CustodyAccountKeySourceTests, HybridPqChannelTests; E2E: none | rename file → `Hybrid/IPqKeyShareClient.cs`; extract `Hybrid/HybridPublicIdentity.cs`, `Hybrid/HybridPrivateIdentity.cs`, `Hybrid/PqKeyShareResponse.cs` | rename + split | Yes |
| ChallengePass/Hybrid | `Hybrid/PqSymmetricChannel.cs` **DONE extract IPqChannel** | IPqChannel(interface), PqSymmetricChannel(class)* | CP: ChallengePassServiceCollectionExtensions, ChannelSealAlgorithm, PqChannelChallengePassStructure; Shell: AppIdleLockService, HttpPqChannelChallengeSource; Tests: HybridPqChannelTests; E2E: none | `Hybrid/IPqChannel.cs` | split only | Yes |
| ChallengePass/Structures | `Structures/PqChannelChallengePassStructure.cs` **DONE extract IPqChannelChallengeSource + InMemoryPqChannelChallengeSource** | IPqChannelChallengeSource(interface), PqChannelChallengePassStructure(class)*, InMemoryPqChannelChallengeSource(class) | Core: InMemoryProductClient; CP: ChallengePassServiceCollectionExtensions, ChallengePassSessionProofBuilder; Shell: HttpPqChannelChallengeSource, MauiProgram; Tests: ChallengePassZeroTokenOverloadTests, HybridPqChannelTests; E2E: none | `Structures/IPqChannelChallengeSource.cs`, `Structures/InMemoryPqChannelChallengeSource.cs` | split only | Yes |

## Totals

- 24 files in `CipherBank-app.Core`, 5 files in `CipherBank-app.ChallengePass` — 29 files, 58 SA1402/SA1649 issues (49 on m1/PR#20, 9 on m2/PR#21).
- No new SA1402/SA1649 issues found beyond these on m1/m2 at time of writing; m3/m4 issue exports were not part of this pass (only `m1`/`m2` existed under `/tmp/sonar-stack`).

## Execution order (once reviewed)

1. Confirm this plan with the team (per spec gate — Stage 3 blocked until then).
2. Real caller search per type (replace `TBD`), starting with `high churn` rows, and covering `Core`/`ChallengePass`, MAUI Shell (per the Shell compile gate above), `CipherBank-app.Tests`, and `CipherBank-app.E2ETests`.
3. Execute renames/splits on `prototype/maui-m1` first (earliest layer touching these files), then merge/cherry-pick up through m2 → m3 → m4, same as Stage 1 mechanical fixes.
4. One PR per layer/folder (e.g. `Core/V1`, `ChallengePass`) to keep diffs reviewable; run the full test suite **and** the Shell build before each push.
