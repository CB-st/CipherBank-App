# Synthetic emulator lab credentials for CipherBank Appium E2E (M4 Story Facts).
# Copy to artifacts/e2e-local.env (gitignored) and replace the placeholders.
# Do not commit real device PINs or recovery passwords.

E2E_TEST_PIN=246810
E2E_TEST_PIN_ALT=135790
E2E_RECOVERY_PASSWORD=correct-horse-battery-staple

# Optional: current,backup SHA-256 SPKI (Base64) to stamp a pin-set into the Appium Debug APK.
# Leave unset to skirt (system CAs) until ops publishes the real pair.
# ANDROID_CERT_PINS=

